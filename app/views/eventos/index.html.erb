<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">  
  <h1 class="h2">Crianças em sala de aula</h1>
  <div class="btn-toolbar mb-2 mb-md-0">
    <div class="btn-group mr-2">
      <%= link_to 'Checkout Todos', checkout_all_path, class: 'btn btn-sm btn-outline-secondary' %>

      <button class="btn btn-sm btn-outline-secondary">Export</button>
    </div>
    <button class="btn btn-sm btn-outline-secondary dropdown-toggle">
      <span data-feather="calendar"></span>
      This week
    </button>
  </div>
</div>

<div class="row">
<% @pessoas.each do |pessoa| %>
  <div class="col-md-3 col-lg-3 col-xl-3 mb-5">
    <div class="card card-tile card-xs bg-success bg-gradient text-center">
      <div class="card-body p-4">
        <!-- Accepts .invisible: Makes the items. Use this only when you want to have an animation called on it later -->
        <div class="tile-right">
          <div class="tile-checkin-titulo"><strong><%= pessoa.nome_completo %></strong></div>
          <div class="tile-checkin-texto"><%= pessoa.turma.nome_turma if Configuracao.where(id: 1, turmas: true).present?  %> 
          <%= '(' + pessoa.sala.nome_sala  + ')' if Configuracao.where(id: 1, salas: true).present? %></div>
          <div class="tile-checkin-texto"><%= pessoa.data_evento.strftime('%d/%m/%Y %H:%M') %></div>
        </div>
      </div>
    </div>
  </div>
<% end %>
</div>

<br>

